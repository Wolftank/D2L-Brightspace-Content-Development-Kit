# front-end/src/public

The actual HTML/CSS/JS the browser downloads and runs: the configurator form, and the client-side JS that calls `fetch()` against the back end and listens to its event stream. This is genuinely what runs in the browser — the front-end server's job (`../server.js`) is just handing these files over, not executing them. Empty stub; not built yet.
