# front-end

Local server + browser configurator/preview. Displays the plain-language turn status, QA gate findings, and pedagogy check findings the back end produces — the checks themselves live in `back-end/src`, not here.

This is a stub — `src/server.js` and `package.json` exist only so the CI workflow in `.github/workflows/ci.yml` has something real to run against.
