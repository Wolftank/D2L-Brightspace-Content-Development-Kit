/* Loaded by dynamic injection from index.html, never by a static <script src>.
   Keeping content out here means prose can be edited and re-uploaded on its own,
   without touching the engine. */
window.RENAMEME_DATA = {
  items: [
    { id: 'one', title: 'Replace me' },
    { id: 'two', title: 'And me' }
  ]
};
