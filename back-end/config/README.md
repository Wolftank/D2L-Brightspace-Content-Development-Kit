# back-end/config

Hosting target (`local` | `vm`) and AI agent (`claude` | `copilot`) as configuration data, so the same back end runs in either mode without maintaining two separate products. Empty stub — the team defines the actual config schema (env vars vs. a config file) during Module 3-5 when the front-end/back-end API boundary is finalized.
