// Stub entry point. Real job (once built): start the HTTP server and wire
// up the routes defined in ../../docs/architecture.md. Real implementation
// TBD by the back-end track -- see routes/, agent/, pipeline/, tilt-udl/,
// and deploy/ for where each piece of that logic will actually live.
console.log('back-end stub OK');
