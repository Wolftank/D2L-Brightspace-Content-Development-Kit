# back-end/src/deploy

The D2L API client that actually pushes a finished build live and reads back confirmation. Flagged as the highest-uncertainty piece of the back end: today's deploy mechanics (in `../../kit/skills/d2l-activity-deployer/SKILL.md`) work by an AI agent driving a real browser through shadow-DOM clicks, which is Claude-specific behavior hiding inside what's supposed to be an agent-agnostic back end. The real fix is likely a direct D2L API client here — the endpoints and auth pattern are already documented in that skill file — rather than routing deploys through whichever agent is configured. Empty stub; not built yet.
